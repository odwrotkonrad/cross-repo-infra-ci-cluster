##[>] 🤖🤖
#[why] the ci project, created by cross-repo/infra/base along with its enabled services and every
#   artifact registry in it. Named here rather than read from base's state: neither root may depend
#   on the other's, which is the whole point of the split
ci_project_id = "staging-499418"

#[why] the group the runners register against
gitlab_group_id = 130724356

#[why] where a quota increase request is answered
quota_contact_email = "odwrotkonrad@gmail.com"
##[<] 🤖🤖
